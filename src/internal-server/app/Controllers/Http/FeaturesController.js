class FeaturesController {
  async default({ response }) {
    return response.send({});
  }

  async features({ response }) {
    return response.send({});
  }
}

module.exports = FeaturesController;
